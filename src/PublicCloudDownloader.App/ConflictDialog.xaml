<Window x:Class="PublicCloudDownloader.App.ConflictDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Existing Files" Width="520" Height="260" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="28"><Grid.RowDefinitions><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel><TextBlock Text="Some files already exist" FontSize="21" FontWeight="SemiBold"/><TextBlock x:Name="Description" Margin="0,12,0,0" TextWrapping="Wrap" Foreground="{StaticResource SecondaryTextBrush}"/><TextBlock Margin="0,12,0,0" Text="Choose one action for all existing files in this download." Foreground="{StaticResource SecondaryTextBrush}"/></StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
            <Button Content="Skip existing" Style="{StaticResource SecondaryButton}" Margin="10,0,0,0" Click="Skip_Click"/>
            <Button Content="Overwrite existing" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Click="Overwrite_Click"/>
        </StackPanel>
    </Grid>
</Window>
